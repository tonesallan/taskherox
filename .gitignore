# .NET
bin/
obj/
publish/
dist/
*.user
*.suo
.vs/

# Rider / VS Code
.idea/
.vscode/

# build/publish output
[Dd]ebug/
[Rr]elease/
artifacts/

# TaskHeroX local backups / migration diagnostics
*.bak
backups/
python_old_project/diag_*.py

# web-preview (mockup do layout) — deps geradas
layout/web-preview/node_modules/
layout/web-preview/.vite/

# offsets cache LOCAL (gerado ao lado do exe). Versionados: os EMBUTIDOS no projeto e o FEED público
# (offsets/), de onde todo painel instalado baixa os offsets quando o jogo atualiza.
offsets_*.json
!src/**/Offsets/offsets_*.json
!offsets/offsets_*.json
*.log
